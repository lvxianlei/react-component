const validateRule = {
    required: '${label}是必填的!',
    types: {
        email: '${label} is not a valid email!',
        number: '${label} is not a valid number!',
    },
}

export default validateRule